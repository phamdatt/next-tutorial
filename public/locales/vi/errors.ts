const errors = {
  unspecific: "Something went wrong.",
  "404": "The page was not found.",
};

// const error = '404';
//i18next.t([`error.${error}`, 'error.unspecific']); // -> "The page was not found"

// const error = '502';
//18next.t([`error.${error}`, 'error.unspecific']); // -> "Something went wrong"
